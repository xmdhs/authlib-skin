version: 2
sql:
  - engine: "mysql"
    schema: "sql/schema.sql"
    queries: "sql/query.sql"
    gen:
      go:
        package: "mysql"
        out: "."
        emit_db_tags: true
        emit_prepared_queries: true
        emit_interface: true
